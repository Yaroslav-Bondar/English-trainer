// import PropTypes from 'prop-types';
import styles from './HomePage.module.css';

const HomePage = () => {
    return (
        <h1>HomePage page</h1>
    );
}

// HomePage.propTypes = {
//     text: PropTypes.
// }

export default HomePage;